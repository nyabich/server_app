const client = require('./ClientController');
const gestionnaire = require('./GestionnaireController');
const commande =  require('./CommandController');

module.exports = {
  client,
  gestionnaire,
  commande
};